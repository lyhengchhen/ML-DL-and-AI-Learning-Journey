Function calling = Give model access to new functionality and data they can use to follow instruction and respond to prompts. 

Function calling = Tool calling:
- Powerful and flexible way for models to interface with external systems and access data outside their traning data. `